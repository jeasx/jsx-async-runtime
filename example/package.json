{
  "name": "jsx-async-runtime-example",
  "type": "module",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "esbuild --format=esm --platform=node --outdir=dist --bundle src/index.js",
    "dev": "nodemon -i dist -e 'js,jsx' start"
  },
  "devDependencies": {
    "@types/node": "latest",
    "esbuild": "latest",
    "jsx-async-runtime": "latest",
    "nodemon": "latest"
  }
}
